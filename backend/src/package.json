{
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
  "dev": "npx concurrently -n BACKEND,FRONTEND -c yellow,cyan \"cargo run --manifest-path ../backend/Cargo.toml\" \"vite\""
}
}
